---
name: doc-format-align
description: "把 Word 文档的格式对齐到指定格式模板（docx），包括字体、标题、标题编号、行距缩进、页边距、页眉页脚页码、表格、目录样式。只改格式，不改正文文字。Use when the user wants to 格式对齐 / 套模板 / 按模板改格式 / 按XX格式排版 / 标题编号不对 / 字体不对 / 行距不对 / 页边距不对 / 正文格式不对 / 把文档整理成公司格式。"
---

# 文档格式对齐（doc-format-align）

把一份 Word 文档的**格式**对齐到指定格式模板：字体字号、标题层级与编号、段落行距缩进、页边距、页眉页脚页码、表格样式、目录样式，全部以模板为准。

**内容保护第一原则：只改格式与版式，正文文字一字不改**（段落文字、顺序、结构原样保留）。唯一的文本改动是"标题编号"——当目标标题编号与模板同级编号不一致时，按模板体系重写编号（编号属于格式范围）。

## 触发与前置检查

1. 确认目标文档与模板：**目标文档路径与格式模板路径都由用户每次输入提供**，不预设默认路径。
   - 完成标准：两个路径都存在且都是 `.docx`。`.doc` 老格式先请用户用 Word 另存为 `.docx`。

2. 定位脚本目录（本 skill 的 `scripts/`）。**绝不用裸相对路径运行脚本**：
   ```bash
   SCRIPTS="<本skill目录>/scripts"
   python "$SCRIPTS/align_format.py" --target <目标.docx> --template <模板.docx>
   python "$SCRIPTS/verify_format.py" --target <目标.docx> --template <模板.docx> --report <报告.md>
   ```

## 对齐步骤

### 第一步：对齐格式（align_format.py）

```bash
python "$SCRIPTS/align_format.py" \
  --target <目标.docx> \
  --template <模板.docx> \
  [--output <输出.docx>] \
  [--number-mode continuous|nested] \
  [--title-shift N]
```

- `--title-shift N`：目标标题层级相对模板偏移 N——目标 H1 用模板 H(1+N) 的格式与编号。
  用于**模板最高层级是封面/章节特有层、需忽略**的场景，如招标文件模板"第一章/第二章"
  （章层）+ "一、招标条件"（节层），目标文档没有"章"、一级标题应对齐模板节层 → `--title-shift 3`
  （模板章层在体系里是 H3，节层 H4，条层 H5）。**对齐前先向用户确认**是否需要偏移。
- 默认**直接覆盖目标文件**，并在同目录生成 `<目标名>_备份.docx`；指定 `--output` 则写新文件且不备份原文件。
- **输出命名固定，不保留中间版本**：用 `--output` 时统一命名为 `<目标名>_对齐版.docx`；
  重复对齐直接覆盖该文件，不要生成 `_v2`/`_v3` 等递增版本。需要暂存过程稿时先让用户手动另存，
  或对齐前请用户确认。
- **备份语义严格限定为"对齐前的原文件快照"**：备份由脚本在覆盖前原样复制目标文件生成，之后不再改动。
  若因脚本缺陷或重跑导致备份不是原文件快照（例如重建的"近原版"），它就不是有效备份——按中间产物处理，
  **一律删除，不得按"原文件"语义保留**。
- **最终交付物默认为"只保留对齐版"**：对齐、验证、检查、目检全部完成后，删除备份与报告
  （除非用户明确要求保留），工作目录只留对齐版 + 模板。详见下文「交付与收尾」。
- **验证临时文件即用即删**：目检/预览产生的临时 PDF、临时 docx（如 `_xxx.pdf`、`_对齐版_new.docx`）、
  `__pycache__` 等在验证完成后**立即删除**。每次任务收尾时检查一次工作目录，确认无 `_` 前缀临时文件残留。
- `--number-mode`：`continuous`（默认，各级标题全文连续编号，如 一、二、三… / 1、2、3…）或 `nested`（子级在上级下重新从 1 计，适合 1.1 / 1.1.1 技术文档）。
- 脚本输出应包括：模板规范摘要、角色统计、编号重编统计、**内容保护校验（必须显示"通过"）**。
- 完成标准：脚本以退出码 0 结束，且输出"内容保护：通过（正文文本零改动）"。若显示警告或退出码非 0，先排查修复再继续，不要交付。

### 第二步：生成对齐报告（verify_format.py）

```bash
python "$SCRIPTS/verify_format.py" \
  --target <对齐后.docx> \
  --template <模板.docx> \
  --report <报告.md> \
  [--number-mode nested]   # 必须与 align_format 的 --number-mode 一致
```

- 报告逐项核对：页面设置、页眉页脚页码、各角色段落格式（H1/H2/H3/H4/正文）、标题编号、表格、目录。
- 完成标准：报告生成成功；把报告内容与用户逐条确认，尤其 ⚠️（需注意）和 🖐（需人工核对）项。
- **报告是过程产物，不是交付物**：确认完毕后删除报告（或询问用户是否保留），不要默认留在工作目录。

### 第三步：目检

- 用 Word 打开对齐后的文档人工核对（字体、编号、分页、页脚页码）。
- 或导出 PDF 预览：在 Word 中「另存为 → PDF」，重点看每页底部页码是否正确、分页是否合理。
- 完成标准：用户确认格式已与模板一致。

## 对齐了什么（以模板为准）

| 项目 | 行为 |
|---|---|
| 封面大标题 TITLE | 文档开头居中的大字号段落（无编号）→ 模板大标题格式（如方正小标宋二号居中） |
| 正文 | 中文字体、字号、行距、首行缩进、对齐方式 → 模板正文格式；**保留目标原有加粗/局部强调**，不覆盖 |
| **图片段落** | 含图片/对象的段落**跳过行距与缩进注入**——模板固定行距会把大图片裁剪到不可见，图片段必须保留自适应行距 |
| **列表段落** | 带自动编号的段落（如"1、xxx"列表）**跳过缩进注入**——缩进由编号定义，强加首行缩进会错乱 |
| **空段落** | 无文本的排版占位段**跳过行距注入**——封面/落款用紧凑行距（如200 twips）空段撑版式，统一注入模板固定行距会撑爆版面（封面溢出成两页） |
| 一级标题 H1 | 字体、字号、加粗、行距、缩进 → 模板一级标题；编号 → 模板一级编号体系（如 `一、`）；**关闭样式自带自动编号**（避免与手动编号重复显示） |
| 二级标题 H2 | 同上，编号如 `（一）` |
| 三级标题 H3 | 同上，编号如 `1、` |
| **第 N 级标题 HN** | 通用规则：模板示范了几级标题，就对齐几级（支持到 8 级）。第 N 级标题 = 模板第 N 级示范的字体/字号/加粗/行距/缩进 + 编号体系；模板没示范的层级，目标对应层级**只对齐字体、不动编号**（报告中提示） |
| 标题编号 | 目标各级标题的段首编号（无论手动文本或 Word 自动编号）按模板同级编号体系重编；样式自带自动编号一律关闭 |
| 页边距/纸张 | 模板的页边距、纸张、页眉页脚距边界 |
| 页眉页脚页码 | 页脚（PAGE 页码域）整体套用模板；**各节原有的页码格式（起始值/罗马数字等）保留不动**；**模板无页眉时移除目标遗留页眉**（严格对齐模板，页眉文字与标题不符时更是如此） |
| 表格 | 套用模板主表样式（模板无表格则不动） |
| 目录 | 不新增、不删改；若目标已有目录，请在 Word 里右键更新目录以套用新样式 |

## 目标文档的角色识别（自动）

按"封面大标题 > 大纲级别 > 标题样式 > 编号样式"识别目标段落属于哪一级标题或正文。
**标题层级数不写死**：模板示范了几级标题（Heading 样式/大纲级别/示范段落编号），就映射几级
（最多 8 级，H1..H8）；模板只有 3 级时用 H1-H3，模板有 5 级 6 级时自动用 H1..H5/H6，
识别与编号规则对所有层级同样适用。

- **封面大标题**：文档开头若干段内居中、无编号、字号≥40（二号）的段落 → TITLE。
- **样式标题体系优先**：文档一旦使用标题样式或大纲级别（如"引言/项目背景"用 Heading 3/4），
  就按样式层级压缩映射到 H1 起的连续层级；此时正文里的带编号列表（"1、xxx""一、xxx"）一律按正文处理，
  **不会被误判成标题**（保护正文列表的关键开关）。
- **正文列表条目保持正文**：正文中的编号列表（自动编号（1）（2）、手动"1、""1."）**保持正文角色，
  套正文格式**（不加粗），编号统一重写为（1）（2）（3）…，**每章内重编**——刻意与标题编号
  （一、/（一）/1、）**不重复**。日期开头的条款正文（"2024年…"）不动。
- **编号样式兜底**：仅当文档完全没有样式标题时（如纯文本排版的公文），才按编号样式推断——
  `一、` → H1、`（一）` → H2、`1、` → H3、`1.1`/`1.1.1` → 对应层级，层级不足自动压缩。
- **正文列表条目保持正文**：正文中的编号列表（自动编号（1）（2）、手动"1、""1."）**保持正文角色，
  套正文格式**（不加粗），编号统一重写为（1）（2）（3）…，**每章内重编**——刻意与标题编号
  （一、/（一）/1、）**不重复**。日期开头的条款正文（"2024年…"）不动。
- 用"加粗+大字号"冒充标题、不带编号的段落会被当作正文；若用户明确要当标题处理，
  见 `references/word-com-notes.md` 的"模板自适应"节。

## 模板未提及项的既定规则（自动执行 + 报告中提示）

模板未定义的部分按以下规则处理，并在验证报告中说明：

| 模板未提及项 | 处理规则 |
|---|---|
| 正文列表编号（如"（1）"自动编号、手动"1、"） | 保持**正文格式**（不加粗），编号重写为（1）（2）（3）…每章内重编，与标题编号不重复 |
| 模板没示范的标题层级（如模板只有 3 级、目标有 4 级） | 该层级只对齐字体、不动编号（报告中提示"模板未定义该层级"） |
| 目录（TOC） | **对齐前先向用户确认**处理方式：默认保留目标目录 + 提示"在 Word 中右键更新域"（模板无目录时也保留）；用户可选删除目录严格对齐模板 |
| 表格样式 | 按样式 name 匹配/复制到目标 styles.xml（不能只复制 styleId 引用，跨文档 styleId 可能冲突） |
| 页眉 | 模板无页眉 → 移除目标遗留页眉（页眉文字与正文标题不符时更要移除）；模板有页眉 → 保留目标页眉供人工核对 |

## 修改后全面检查（确定性检查脚本）

对齐完成后**必须运行 `check_result.py` 做全面自动检查**（秒级完成，无需启动 Word）：

```bash
python "$SCRIPTS/check_result.py" \
  --target <对齐版.docx> \
  --template <模板.docx> \
  --original <原文件.docx> \
  --number-mode nested   # 与 align 一致
```

覆盖 8 项：① 内容保护（原 vs 对齐版去编号文本一致）② 图片完整性（media/drawing/图片关系数与原一致）
③ 标题编号（与模板体系一致、无样式自动编号残留）④ 页眉页脚（页眉符合模板、页脚 PAGE 域）
⑤ 页面设置（pgSz/pgMar 与模板一致）⑥ 正文格式（抽查字体/字号符合规范）⑦ 文档完整性（XML 全可解析）
⑧ 目录（TOC 域/书签保留，提示更新域）。

- **完成标准：8 项全部 ✅**。任一项 ❌ 都要修复后重跑对齐并复检，直到全绿。
- 封面分页、图片实际渲染这类需要"所见即所得"的项，用 Word 打开对齐版目检（或导出 PDF 看分页/页码）。
- 只有遇到上述确定性检查无法覆盖、且需要独立第三方视角的疑难情况，才派子 agent 专项核查；
  不要为常规对齐启动重型子 agent 复查（耗时长、非必需）。

## 交付与收尾（每任务必做）

对齐、验证、检查、目检全部完成后，按以下清单收尾：

1. **只保留结果文件**：删除备份、报告、临时 PDF/docx、`__pycache__` 等一切中间产物；
   工作目录只留对齐版 + 模板。除非用户明确要求保留某项。
2. **备份必须是真的"对齐前原文件"**：若对齐过程中原文件被覆盖且无严格原文件快照
   （如脚本缺陷重跑后的产物），该"备份"不成立——一律删除，并向用户如实说明原文件已不存在、无法回滚。
3. **列出交付物清单**：向用户展示最终保留的文件，确认无多余文件后再结束任务。
4. **如实报告**：过程中任何偏离（脚本缺陷修复、原文件覆盖、备份不可用等）都要向用户透明说明，
   不得静默处理或把非原文件快照冒充"备份"保留。

## 本 skill 自身的修改（防漂移守则）

SKILL.md 与 `scripts/` 是这个 skill 的**行为定义**：改一处即改全局行为，而对齐质量全部依赖
脚本的确定性（内容保护、编号体系、页面设置都由脚本保证）。脚本缺陷曾直接造成交付问题
（标题编号丢失、备份语义失效），所以脚本不是随手可改的东西。

只有三类情况允许改动，其余一律先向用户确认：

1. **用户明确要求修改**——直接做，但改完要向用户说明"改了什么、为什么改"。
2. **新模板形态不在既有逻辑覆盖内**（如模板标题编号是 Word 自动编号、正文用特殊样式）——
   先读 `scripts/ooxml_util.py` 模块说明和相关函数，理解现状再动手，改前说明原因。
3. **发现脚本缺陷**——先复现、定位根因，向用户说明缺陷表现与修复方案，改完重跑全链路验证。

任何修改都必须遵守：

- **先读后改**：修改脚本前先读 `scripts/ooxml_util.py` 的模块说明，保持"只动格式不动文本"的
  结构，不破坏内容保护第一原则——脚本改坏了，正文就被动了。
- **改前留底**：修改脚本前先备份原文件（或确认已有备份），改坏可回退，不留不可逆改动。
- **改后必验**：脚本改动后必须重跑 对齐 → 验证报告 → 全面检查（8 项全 ✅ + 内容保护"通过"），
  不验证不交付——改脚本的价值只有跑通全链路才算数。
- **透明报告**：向用户说明"改了什么、为什么改、影响什么"，不静默修改、不留无主改动；
  宁可多解释一句，也不要让用户事后发现 skill 被改了却不知情。

## 参考

- 技术细节、模板形态与已知限制见 [references/word-com-notes.md](./references/word-com-notes.md)。
